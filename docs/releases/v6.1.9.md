# v6.1.9

Release date: 2026-07-02

## Summary

This patch release aligns runtime selection across CI and local development by moving workflows to the latest stable Node version and adding a local Node version hint.

## Highlights

- Updated CI workflows to use the latest stable Node runtime (`node`) instead of pinning Node 20.
- Added explicit Node setup in the release workflow for consistent runtime behavior.
- Added `.nvmrc` with `node` so local `nvm use` matches CI defaults.
- Keeps release and validation tooling aligned as Node deprecations evolve.

## Included Updates

- Updated Node setup in `.github/workflows/e2e.yml`.
- Updated Node setup in `.github/workflows/accessibility.yml`.
- Added Node setup in `.github/workflows/release.yml`.
- Added `.nvmrc` for local runtime alignment.
- Updated release metadata in `package.json`, `index.html`, `CHANGELOG.md`, `README.md`, and `docs/releases/README.md`.

## Accessibility Compliance

- Standard target: WCAG 2.2.
- This release modifies workflow/runtime configuration only; no accessibility interaction changes.

## Validation

- `npm run test` passed.

## Tag

- `v6.1.9`
